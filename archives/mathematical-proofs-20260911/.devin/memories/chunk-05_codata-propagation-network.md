# Chunk 05: CODATA Propagation Network

**Source:** `x.md` lines 135–162
**Category:** codata-network
**Status:** established

---

## Concepts Extracted

- **α as propagation hub**: The fine-structure constant isn't just an arbitrary number — it propagates into many other constants
- **Power hierarchy**: Different constants carry different powers of α (a_0∝α^-1, r_e∝α^-3, σ_e∝α^-6)
- **Framework mapping**: The signed dimensional propagation in the framework directly maps to α-power propagation
- **Better experiment**: Instead of fitting each constant independently, derive α's signature and propagate

---

## Mathematical Structure

### Fine-Structure Constant Definition
$$ \boxed{\alpha = \frac{e^2}{4\pi\epsilon_0\hbar c}} $$

### α-Power Propagation
$$ a_0 \propto \alpha^{-1} $$
$$ \lambda_C \propto \alpha^{-2} $$
$$ r_e \propto \alpha^{-3} $$
$$ \sigma_e \propto \alpha^{-6} $$

### Better Test Formulation
$$ \boxed{\text{What dimensional signature does the system assign to } \alpha?} $$

Then: signature → φ^a + π^b + φ^c → α, and propagate α^-1, α^-2, α^-3, α^-6.

---

## Key Results

1. α propagates into at least 5 major constants (R_∞, a_0, λ_C, r_e, σ_e) with powers -1, -2, -3, -6
2. The framework's signed dimensional propagation directly maps to this α-power hierarchy
3. The correct experiment is to derive α's dimensional signature, generate α, then propagate — not fit each constant independently
4. If one fixed graph produces α, R_∞, a_0, and the hydrogen line simultaneously, that's the zero-free-parameter result

---

## Cross-References

- **chunk-04**: The naive α attempt fails because α has this special propagation structure
- **chunk-06**: The dimensional propagation graph (S^1, N^3, S^3, ...) formalizes signature paths
- **chunk-07**: CODATA consistency relationships give exact formulas (R_∞ = α²m_ec/(2h), etc.)
- **chunk-21**: The EM over-constraint chain derives the entire family from a single α

---

## Open Questions

- What dimensional signature does the propagation graph assign to α?
- Can the same fixed graph produce α AND its propagated powers simultaneously?
- Does the propagation close through e_7 → e_0/e_8?
